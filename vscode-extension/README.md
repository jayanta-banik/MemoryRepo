# MemoryRepo

MemoryRepo is a VS Code extension for building memory from code repositories.

Run the `MemoryRepo: Hello` command to show:

```text
Hello from MemoryRepo
```

When a workspace folder is open, the command uses that folder name.
